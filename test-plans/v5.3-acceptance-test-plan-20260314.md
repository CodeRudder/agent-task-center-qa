# V5.3版本验收测试计划

**版本**: V5.3  
**测试日期**: 2026-03-14  
**测试环境**: localhost:3103（合并端口）  
**测试人员**: QA工程师  

---

## 📋 验收范围

### P2-3: 任务依赖关系功能
- **前端文件**: `/src/pages/TaskDependencies/index.tsx`
- **后端API**: 
  - `GET /api/v1/tasks/:id/dependencies` - 获取任务依赖关系
  - `POST /api/v1/tasks/:id/dependencies` - 设置任务依赖关系
  - `DELETE /api/v1/tasks/:id/dependencies/:depId` - 删除依赖关系

### P2-4: 标签与分类功能
- **前端文件**: `TagManagementPage.tsx`（345行）
- **后端API**:
  - `POST /api/v1/tags` - 创建标签
  - `GET /api/v1/tags` - 获取标签列表
  - `PATCH /api/v1/tags/:id` - 更新标签
  - `DELETE /api/v1/tags/:id` - 删除标签
  - `POST /api/v1/categories` - 创建分类
  - `GET /api/v1/categories` - 获取分类列表
  - `PATCH /api/v1/categories/:id` - 更新分类
  - `DELETE /api/v1/categories/:id` - 删除分类

### P2-5: 报表统计功能
- **前端文件**: `ReportStatisticsPage.tsx`（490行）
- **后端API**:
  - `GET /api/v1/statistics/tasks` - 任务统计
  - `GET /api/v1/statistics/workload` - 工作量统计
  - `GET /api/v1/statistics/trends` - 趋势分析
  - `GET /api/v1/statistics/export/csv` - CSV导出

---

## 🎯 验收测试用例

### P2-3: 任务依赖关系功能测试用例

#### TC-P2-3-001: 查看任务依赖关系
- **测试步骤**:
  1. 登录系统
  2. 进入任务详情页
  3. 点击"依赖关系"按钮
  4. 查看当前任务的依赖关系列表
- **预期结果**:
  - 显示当前任务的所有依赖关系
  - 每个依赖关系显示：依赖任务标题、状态、依赖类型
  - 显示阻塞状态（已完成的依赖不阻塞，未完成的依赖阻塞）

#### TC-P2-3-002: 添加任务依赖关系
- **测试步骤**:
  1. 进入任务依赖关系页面
  2. 点击"添加依赖"按钮
  3. 选择一个依赖任务
  4. 选择依赖类型（FS/SS）
  5. 点击"确定"
- **预期结果**:
  - 依赖关系添加成功
  - 依赖关系列表更新
  - 显示成功提示消息

#### TC-P2-3-003: 删除任务依赖关系
- **测试步骤**:
  1. 进入任务依赖关系页面
  2. 点击依赖关系旁边的"删除"按钮
  3. 确认删除
- **预期结果**:
  - 依赖关系删除成功
  - 依赖关系列表更新
  - 显示成功提示消息

#### TC-P2-3-004: 阻塞状态验证
- **测试步骤**:
  1. 添加一个未完成的依赖任务
  2. 查看当前任务的阻塞状态
  3. 完成依赖任务
  4. 再次查看阻塞状态
- **预期结果**:
  - 未完成依赖时，显示"被阻塞"状态
  - 依赖完成后，显示"未阻塞"状态

#### TC-P2-3-005: 依赖类型验证
- **测试步骤**:
  1. 添加FS类型依赖
  2. 添加SS类型依赖
  3. 查看依赖关系列表
- **预期结果**:
  - FS类型显示"完成后开始"
  - SS类型显示"同时开始"

---

### P2-4: 标签与分类功能测试用例

#### TC-P2-4-001: 创建标签
- **测试步骤**:
  1. 进入标签管理页面
  2. 点击"创建标签"按钮
  3. 输入标签名称
  4. 选择标签颜色
  5. 选择分类（可选）
  6. 点击"确定"
- **预期结果**:
  - 标签创建成功
  - 标签列表更新
  - 显示成功提示消息

#### TC-P2-4-002: 查看标签列表
- **测试步骤**:
  1. 进入标签管理页面
  2. 查看标签列表
- **预期结果**:
  - 显示所有标签
  - 每个标签显示：名称、颜色、分类
  - 支持分页

#### TC-P2-4-003: 更新标签
- **测试步骤**:
  1. 进入标签管理页面
  2. 点击标签旁边的"编辑"按钮
  3. 修改标签信息
  4. 点击"确定"
- **预期结果**:
  - 标签更新成功
  - 标签列表更新
  - 显示成功提示消息

#### TC-P2-4-004: 删除标签
- **测试步骤**:
  1. 进入标签管理页面
  2. 点击标签旁边的"删除"按钮
  3. 确认删除
- **预期结果**:
  - 标签删除成功
  - 标签列表更新
  - 显示成功提示消息

#### TC-P2-4-005: 分类过滤
- **测试步骤**:
  1. 进入标签管理页面
  2. 选择一个分类进行过滤
  3. 查看过滤结果
- **预期结果**:
  - 只显示选定分类的标签
  - 过滤功能正常

#### TC-P2-4-006: 标签搜索
- **测试步骤**:
  1. 进入标签管理页面
  2. 在搜索框输入标签名称
  3. 查看搜索结果
- **预期结果**:
  - 显示匹配的标签
  - 搜索功能正常

---

### P2-5: 报表统计功能测试用例

#### TC-P2-5-001: 任务统计
- **测试步骤**:
  1. 进入报表统计页面
  2. 查看任务统计图表
  3. 验证统计数据的准确性
- **预期结果**:
  - 显示任务统计图表
  - 数据准确（按状态、优先级等统计）
  - 图表显示正常

#### TC-P2-5-002: 工作量统计
- **测试步骤**:
  1. 进入报表统计页面
  2. 查看工作量统计图表
  3. 验证统计数据的准确性
- **预期结果**:
  - 显示工作量统计图表
  - 数据准确（按Agent、时间段等统计）
  - 图表显示正常

#### TC-P2-5-003: 趋势分析
- **测试步骤**:
  1. 进入报表统计页面
  2. 查看趋势分析图表
  3. 选择不同的时间范围
  4. 验证趋势数据的准确性
- **预期结果**:
  - 显示趋势分析图表
  - 数据准确（按天、周、月等统计）
  - 时间范围切换正常

#### TC-P2-5-004: CSV导出
- **测试步骤**:
  1. 进入报表统计页面
  2. 点击"导出CSV"按钮
  3. 下载CSV文件
  4. 验证CSV文件内容
- **预期结果**:
  - CSV文件下载成功
  - 文件内容正确
  - 包含所有统计数据

---

## 🔍 验收测试环境

### 测试环境配置
- **前端地址**: http://localhost:3103
- **后端API**: http://localhost:3103/api/v1
- **数据库**: PostgreSQL
- **浏览器**: Chrome/Firefox

### 测试数据准备
- 创建测试用户
- 创建测试任务
- 创建测试标签和分类
- 创建测试依赖关系

---

## 📊 验收标准

### 功能完整性
- ✅ 所有功能点都已实现
- ✅ 所有API端点都已实现
- ✅ 前后端集成正确

### UI交互
- ✅ UI符合设计稿
- ✅ 交互流畅
- ✅ 响应式设计正常

### 数据准确性
- ✅ 统计数据准确
- ✅ 数据显示正确
- ✅ 数据导出正确

### 性能
- ✅ 页面加载速度正常
- ✅ API响应时间正常
- ✅ 图表渲染速度正常

---

## 🚨 风险点

### P2-3: 任务依赖关系
- 循环依赖检测（需要验证）
- 依赖关系删除后任务状态更新
- 大量依赖时的性能

### P2-4: 标签与分类
- 标签颜色选择器兼容性
- 分类过滤性能
- 标签删除后的任务关联处理

### P2-5: 报表统计
- 大数据量时的图表性能
- CSV导出文件大小限制
- 统计数据实时性

---

## 📝 验收测试报告

### 测试执行时间
- 开始时间: 待定（等待部署完成）
- 结束时间: 待定

### 测试结果
- 待测试完成后填写

### 问题记录
- 待测试完成后填写

### 验收结论
- 待测试完成后填写

---

**更新时间**: 2026-03-14 20:56  
**状态**: 🔄 验收测试准备中（等待部署完成）
